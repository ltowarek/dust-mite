apiVersion: 1

datasources:
  - name: Tempo
    uid: tempo
    type: tempo
    access: proxy
    url: http://tempo:3200
    isDefault: true
    editable: true
    jsonData:
      tracesToProfiles:
        datasourceUid: pyroscope
        profileTypeId: "samples:samples:count:samples:count"
        customQuery: false
        tags:
          - key: "service.name"
            value: "service_name"
